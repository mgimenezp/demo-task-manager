
# Demo Task Manager

Aplicación web Java simple con Spring Boot, Jenkins y Git.

## 🚀 Ejecución local

1. Clonar el repositorio
2. Ejecutar:
   ```bash
   mvn spring-boot:run
   ```
3. Abrir [http://localhost:8088](http://localhost:8088)

## ⚙️ Jenkins

Pipeline: build → test → deploy

## 🧪 Test unitario

Ejecuta un test real con JUnit5 que valida la creación de tareas.

## 💾 Deploy local

Simula un entorno de producción copiando el `.jar` a `deploy/produccion`.
